name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.2.3'
      SERVERPOD_PASSWORD_database: 'v2NDB6T0okRTkGiAS4lw6_cgTzZSJ2WJ'
      SERVERPOD_PASSWORD_redis: 'zipGam9xKiT217i1egeaEBONEAABk6px'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: receipt_backend_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: receipt_backend_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: receipt_backend_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: receipt_backend_server
        run: serverpod generate

      - name: Run tests
        working-directory: receipt_backend_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: receipt_backend_server
        run: docker compose down -v
